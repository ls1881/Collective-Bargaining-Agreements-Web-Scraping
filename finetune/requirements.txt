# Local-only steps (data_prep.py, distill_labels.py, review_sample.py, eval post-processing):
# runs fine on the M3 Max, no CUDA/bitsandbytes needed.
pandas
numpy
anthropic
tqdm
jiwer
jsonschema

# GPU-only steps (train_clean_lora.py, train_extract_lora.py, infer_batch.py):
# install these only on the rented CUDA GPU instance.
torch>=2.3
transformers>=4.44
peft>=0.12
bitsandbytes>=0.43
trl>=0.9
accelerate>=0.33
datasets
sentencepiece
vllm>=0.5
wandb
